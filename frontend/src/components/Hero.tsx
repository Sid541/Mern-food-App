import hero from "../assets/hero.png";
import { Button } from "./ui/button";

const Hero = () => {
  return (
    
    <div>
      <img src={hero} alt="" style={{ width: '100%' }} />
      
    </div>
    
  );
};

export default Hero;
